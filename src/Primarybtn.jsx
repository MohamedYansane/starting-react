import React from 'react'

export const Primarybtn = ({btn_name}) => {
  return (
    <button type="submit" className="p-btn">{btn_name}</button>

  )
}

export const BtnName = ({btn_name}) =>{
    return <button type="submit" className="p-btn">{btn_name}</button>

}
